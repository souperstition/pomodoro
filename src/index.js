import * as ReactDOM from 'react-dom';
import * as React from 'react';
import App from './App';

// fcc tests don't pass with new render method
ReactDOM.render(<App />, document.getElementById('root'));
